<?php
    $this->headTitle(" Acceso restringido -- UNEFA -- SISTES");
    $form = $this->loginForm;
    $form->prepare();
?>
<div class="container">
    <div class="row">
        <div class="col-sm-6 col-md-4 col-md-offset-4">
            <h1 class="text-center login-title">Inicio de Session -- UNEFA</h1>
            <div class="account-wall">
                <img class="profile-img" src="<?php echo $this->basePath(). '../../img/logo.png';?>"
                    alt="">
                <?php
                $form = $this->loginForm;
                $form->prepare();
                $form->setAttribute('action', $this->url('zfcuser/login'));
                $form->setAttribute('method', 'post');
                $form->setAttribute('class', 'form-signin');
                echo $this->form()->openTag($form)
                ?>
                    <input type="text" id="identity" name="identity" class="form-control" placeholder="Usuario" required autofocus>
                    <input autocomplete="off" type="password" id="credential" name="credential" class="form-control" placeholder="Password" required>
                    <button class="btn btn-lg btn-primary btn-block" type="submit">Acceder</button>
                    <?php if ($this->redirect): ?>
                    <input autocomplete="off" type="hidden" name="redirect" value="<?php echo $this->redirect ?>" />
                    <?php endif ?>
                <?php echo $this->form()->closeTag() ?>
                <span class="label-danger"><?php echo $this->formElementErrors($form->get('identity')) ?></span>
            </div>
        </div>
    </div>
</div>
